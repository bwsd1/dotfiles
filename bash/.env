#!/bin/bash
export BROWSER=google-chrome
export EDITOR=vim
export VISUAL=$EDITOR
export GIT_EDITOR=$EDITOR
export FORTUNES="$HOME/fortunes"

export LANG="en_US.UTF-8"
export LANGUAGE="en_US:en"

# disable termcap deinit string from clearing screen on close
export MANPAGER="less -X"

# Default GPG key for gpg-pubkey(), specified by fingerprint or key ID.
# Left empty here so the file stays machine-independent; set it in a
# local override if you want gpg-pubkey to work without an argument.
export KEY=

export PASSWORD_STORE_DIR="$HOME/password-store"

export XDG_CONFIG_HOME="$HOME/.config"
export XDG_CACHE_HOME="$HOME/.cache"
export XDG_DATA_HOME="$HOME/.local/share"

export DOCKER_BUILDKIT=1

export GOPATH="$HOME/go"

export HADOLINT_IGNORE="DL3018,DL3020"
export SHELLCHECK_OPTS='-fgcc --exclude=SC1090,SC1117'
